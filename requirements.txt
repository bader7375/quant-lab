numpy>=1.26
pandas>=2.1
scipy>=1.11
scikit-learn>=1.4
lightgbm>=4.1
xgboost>=2.0
catboost>=1.2
optuna>=3.5
yfinance>=0.2.40
pyarrow>=14.0
matplotlib>=3.8
PyYAML>=6.0
pytest>=7.4
tabulate>=0.9
openpyxl>=3.1
joblib>=1.3

# Optional. Every one of these degrades gracefully if it is missing: the
# sequence models are skipped without torch, SHAP explanations fall back to
# LightGBM gain, and the regime engine uses k-means alone without hmmlearn.
# torch is the heavy one -- for a CPU-only install use
#   pip install torch --index-url https://download.pytorch.org/whl/cpu
torch>=2.2
shap>=0.44
hmmlearn>=0.3
